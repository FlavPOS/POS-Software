name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "lib/**"
      - "android/**"
      - "pubspec.yaml"
      - "pubspec.lock"
      - ".github/workflows/build-android-apk.yml"

permissions:
  contents: write

jobs:
  build-android:
    name: Analyze, Test, and Build ARM64 APK
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      JAVA_TOOL_OPTIONS: >-
        -Xmx1536m
        -XX:MaxMetaspaceSize=512m
        -Dfile.encoding=UTF-8
      GRADLE_OPTS: >-
        -Dorg.gradle.daemon=false
        -Dorg.gradle.parallel=false
        -Dorg.gradle.workers.max=1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.47.0"
          channel: stable
          cache: true

      - name: Show environment
        run: |
          java -version
          flutter --version
          flutter doctor -v
          free -h
          df -h

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Analyze source code
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Build ARM64 release APK
        run: |
          flutter build apk \
            --release \
            --target-platform android-arm64 \
            --split-per-abi

      - name: Verify APK
        run: |
          find build/app/outputs -type f -name "*.apk" -ls
          test -f build/app/outputs/flutter-apk/app-arm64-v8a-release.apk


      - name: Prepare release APK
        run: |
          cp             build/app/outputs/flutter-apk/app-arm64-v8a-release.apk             Simple-POS-arm64.apk

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v0.1.${{ github.run_number }}"

          gh release create "$TAG"             Simple-POS-arm64.apk             --title "Simple POS $TAG"             --notes "Simple POS ARM64 Android release built from commit ${{ github.sha }}"             --latest


      - name: Upload ARM64 APK
        uses: actions/upload-artifact@v4
        with:
          name: simple-pos-arm64-release
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
          if-no-files-found: error
          retention-days: 14
